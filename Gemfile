source "http://rubygems.org"

gem 'haiti'
gem 'rake'
gem 'highline',  '~> 1.6.0'
gem 'json',      '~> 1.7.7' # a dep of something I am apparently using, specified here for versioning reasons
gem 'encryptor', '~> 1.1.3' # for now, but as soon as I get this working, remove this dep (it's only like 10 loc that I need, the real work is done in openssl from the stdlib)
gem 'pry'
gem 'pasteboard'
gem 'surrogate' # ra ra ra!
